# Project Submission

**Class Roll**: 2506132

## 1. Project GitHub URL
This repository contains both the **Frontend** and **Backend** code.

**Repository URL**: `https://github.com/DarusSalamSakib/news-portal-backend`

### Directory Structure
- `/` (Root): Contains Frontend HTML/CSS/JS files.
- `/news-portal-backend`: Contains Node.js/Express Backend code.

### Setup Instructions

#### Backend
1. Navigate to the backend directory: `cd news-portal-backend`
2. Install dependencies: `npm install`
3. Start the server (runs on port 3000): `node server.js`

#### Frontend
1. Open `index.html` in your browser.
2. Ensure the backend is running on port 3000.
3. Use the **new Login/Register forms** to authenticate.
4. **Create News** is now protected and requires login.

## 2. API Test Cases

### Protected Endpoint: Create News
The endpoint `POST /api/news` requires a valid Bearer Token.

```text
--- Testing http://localhost:3000/auth/login (POST) ---
Status: 200
Response: {
  "message": "Login successful",
  ...
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}

--- Testing http://localhost:3000/api/news (POST) ---
Status: 201
Response: {
  "id": 1738944567890,
  "title": "Protected News",
  "body": "This is a protected news article created via API.",
  "author_id": 1,
  "comments": []
}
```

### Full Test Execution Output
*(See `news-portal-backend/api_test_results_protected.txt` for full raw logs)*
